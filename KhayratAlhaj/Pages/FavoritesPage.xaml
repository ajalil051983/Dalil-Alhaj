<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:res="clr-namespace:KhayratAlhaj.Resources.Localization"
             xmlns:local="clr-namespace:KhayratAlhaj.Pages"
             x:Class="KhayratAlhaj.Pages.FavoritesPage"
             Title="{x:Static res:AppResources.FavoritesTitle}"
             BackgroundColor="{AppThemeBinding Light={StaticResource PageBackgroundColorLight}, Dark={StaticResource PageBackgroundColorDark}}">

    <Grid RowDefinitions="*">

        <!-- Favorites List -->
        <ScrollView x:Name="FavoritesScroll" IsVisible="False">
            <VerticalStackLayout Padding="20" Spacing="15">

                <Label x:Name="FavoritesTitleLabel"
                       Text="{x:Static res:AppResources.FavoritesTitle}"
                       FontSize="20"
                       FontAttributes="Bold"
                       TextColor="{AppThemeBinding Light={StaticResource PrimaryTextColorLight}, Dark={StaticResource PrimaryTextColorDark}}"
                       Margin="0,0,0,5" />

                <CollectionView x:Name="FavoritesCollection"
                                SelectionMode="None">
                    <CollectionView.ItemsLayout>
                        <LinearItemsLayout Orientation="Vertical" ItemSpacing="12" />
                    </CollectionView.ItemsLayout>

                    <CollectionView.ItemTemplate>
                        <DataTemplate x:DataType="local:FavoriteItem">
                            <Border Stroke="#BDC3C7"
                                    StrokeThickness="1"
                                    Background="{AppThemeBinding Light={StaticResource CardBackgroundColorLight}, Dark={StaticResource CardBackgroundColorDark}}"
                                    Padding="15"
                                    StrokeShape="RoundRectangle 15">
                                <Border.Shadow>
                                    <Shadow Brush="Black" Opacity="0.15" Radius="6" Offset="0,2" />
                                </Border.Shadow>
                                <Border.GestureRecognizers>
                                    <TapGestureRecognizer Tapped="OnFavoriteItemTapped"
                                                          CommandParameter="{Binding .}" />
                                </Border.GestureRecognizers>

                                <Grid ColumnDefinitions="Auto,*,Auto"
                                      ColumnSpacing="12">

                                    <!-- Category color indicator -->
                                    <BoxView Grid.Column="0"
                                             Color="{Binding CategoryColor}"
                                             WidthRequest="5"
                                             CornerRadius="3"
                                             VerticalOptions="Fill" />

                                    <!-- Text block -->
                                    <VerticalStackLayout Grid.Column="1"
                                                         Spacing="4"
                                                         VerticalOptions="Center">
                                        <Label Text="{Binding SubCategoryName}"
                                               FontSize="16"
                                               FontAttributes="Bold"
                                               TextColor="{AppThemeBinding Light={StaticResource PrimaryTextColorLight}, Dark={StaticResource PrimaryTextColorDark}}"
                                               LineBreakMode="WordWrap" />
                                        <Label Text="{Binding CategoryName}"
                                               FontSize="13"
                                               TextColor="{AppThemeBinding Light={StaticResource PlaceholderTextColorLight}, Dark={StaticResource PlaceholderTextColorDark}}" />
                                    </VerticalStackLayout>

                                    <!-- Icon -->
                                    <Label Grid.Column="2"
                                           Text="{Binding Icon}"
                                           FontSize="28"
                                           VerticalOptions="Center" />
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>

            </VerticalStackLayout>
        </ScrollView>

        <!-- Empty State -->
        <VerticalStackLayout x:Name="EmptyState"
                             IsVisible="True"
                             VerticalOptions="Center"
                             HorizontalOptions="Center"
                             Spacing="20"
                             Padding="40">
            <Label Text="⭐"
                   FontSize="72"
                   HorizontalOptions="Center" />
            <Label Text="{x:Static res:AppResources.NoFavoritesMessage}"
                   FontSize="16"
                   HorizontalOptions="Center"
                   HorizontalTextAlignment="Center"
                   TextColor="{AppThemeBinding Light={StaticResource PlaceholderTextColorLight}, Dark={StaticResource PlaceholderTextColorDark}}"
                   LineBreakMode="WordWrap" />
        </VerticalStackLayout>

    </Grid>

</ContentPage>
