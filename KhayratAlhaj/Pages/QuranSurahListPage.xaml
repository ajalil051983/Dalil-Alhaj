<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:KhayratAlhaj.Pages"
             x:Class="KhayratAlhaj.Pages.QuranSurahListPage"
             BackgroundColor="{AppThemeBinding Light={StaticResource PageBackgroundColorLight}, Dark={StaticResource PageBackgroundColorDark}}">

    <Grid RowDefinitions="Auto,Auto,*" Padding="14" RowSpacing="8">
        <Grid Grid.Row="0"
              ColumnDefinitions="52,*,Auto"
              ColumnSpacing="10"
              Padding="10,8"
              BackgroundColor="{AppThemeBinding Light=#E8E8EC, Dark=#2A2A30}">
                        <Label x:Name="SurahNumberHeaderLabel"
                   Grid.Column="0"
                   FontAttributes="Bold"
                   HorizontalTextAlignment="Center"
                   VerticalTextAlignment="Center" />
                        <Label x:Name="SurahNameHeaderLabel"
                   Grid.Column="1"
                   FontAttributes="Bold"
                   FontSize="18"
                   HorizontalTextAlignment="Start"
                   VerticalTextAlignment="Center" />
                        <Button x:Name="BookmarksButton"
                   Grid.Column="2"
                   Text="🔖"
                   FontSize="18"
                   WidthRequest="46"
                   HeightRequest="38"
                   Padding="0"
                   BackgroundColor="Transparent"
                   Clicked="OnBookmarksClicked" />
        </Grid>

        <SearchBar Grid.Row="1"
                   x:Name="SurahSearchBar"
                   Placeholder=""
                   TextChanged="OnSearchTextChanged"
                   Margin="0,4,0,0" />

        <CollectionView Grid.Row="2"
                        x:Name="SurahCollection"
                        SelectionMode="None"
                        RemainingItemsThreshold="2"
                        RemainingItemsThresholdReached="OnRemainingItemsThresholdReached">
            <CollectionView.ItemsLayout>
                            <LinearItemsLayout Orientation="Vertical" ItemSpacing="6" />
            </CollectionView.ItemsLayout>
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="local:QuranSurahListItem">
                    <Grid ColumnDefinitions="52,*"
                          ColumnSpacing="10"
                          Padding="10,8"
                          BackgroundColor="{Binding RowBackgroundColor}">
                        <Grid.GestureRecognizers>
                            <TapGestureRecognizer Tapped="OnSurahTapped" CommandParameter="{Binding .}" />
                        </Grid.GestureRecognizers>

                        <Label Grid.Column="0"
                               Text="{Binding NumberText}"
                               HorizontalTextAlignment="Center"
                               VerticalTextAlignment="Center"
                               FontAttributes="Bold" />

                        <Label Grid.Column="1"
                               Text="{Binding SurahName}"
                               FontSize="20"
                               HorizontalOptions="Fill"
                               HorizontalTextAlignment="Start"
                               VerticalTextAlignment="Center"
                               TextColor="#0066CC"
                               FontAttributes="Bold"
                               LineBreakMode="TailTruncation" />
                    </Grid>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <Grid x:Name="LoadingOverlay"
              Grid.RowSpan="3"
              IsVisible="False"
              BackgroundColor="#88000000">
            <Border VerticalOptions="Center"
                    HorizontalOptions="Center"
                    BackgroundColor="{AppThemeBinding Light=#FDF6E3, Dark=#25201A}"
                    Stroke="{AppThemeBinding Light=#D4C48A, Dark=#7A6E3F}"
                    StrokeThickness="1"
                    StrokeShape="RoundRectangle 16"
                    Padding="32,24"
                    MinimumWidthRequest="160">
                <VerticalStackLayout Spacing="16" HorizontalOptions="Center">
                    <ActivityIndicator IsRunning="True"
                                       WidthRequest="48"
                                       HeightRequest="48"
                                       Color="{AppThemeBinding Light=#0F5132, Dark=#4CAF80}"
                                       HorizontalOptions="Center" />
                          <Label x:Name="LoadingLabel"
                                        Text=""
                           FontSize="15"
                           HorizontalTextAlignment="Center"
                           TextColor="{AppThemeBinding Light=#5B4210, Dark=#F1D98A}" />
                </VerticalStackLayout>
            </Border>
        </Grid>
    </Grid>
</ContentPage>
