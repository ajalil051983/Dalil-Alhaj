<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
	<application android:allowBackup="false" android:supportsRtl="true" android:icon="@mipmap/kaaba" android:roundIcon="@mipmap/kaaba_round">
		<!-- Google Maps API Key - Not needed for OpenStreetMap (Mapsui) -->
		<!-- Keeps the mushaf page download alive when the app is backgrounded (Android 14+). -->
		<service android:name="com.ilafalkhayr.zadalhaj.DownloadForegroundService"
				 android:exported="false"
				 android:foregroundServiceType="dataSync" />
	</application>
	<!-- Network and Location Permissions -->
	<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
	<uses-permission android:name="android.permission.INTERNET" />
	<uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
	<uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

	<!-- Media and Audio Permissions -->
	<uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
	<uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
	<uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
	<uses-permission android:name="android.permission.WAKE_LOCK" />
	<uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
	<uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="29" />
	
	<!-- Notification Permissions (Prayer Times) -->
	<uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
	<uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
	<uses-permission android:name="android.permission.USE_EXACT_ALARM" />
	<uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
	
	<!-- Appium UI Testing Support - Allow interaction with UiAutomator2 server and settings -->
	<queries>
		<package android:name="io.appium.uiautomator2.server" />
		<package android:name="io.appium.uiautomator2.server.test" />
		<package android:name="io.appium.settings" />
	</queries>
</manifest>
